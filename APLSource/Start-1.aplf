 Start←{
 ⍝ ⍺ ←→ client
 ⍝ ⍵ ←→ 0
     c←⍺
     ride←'SERVE:',c.Host,':',⍕c.Port
     c.Proc←⎕NEW APLProcess('' '' 0 ride)
     _←⎕DL 0.5
     r←DRC.Clt''c.Host c.Port'raw'
     0≠⊃r:∘∘∘
     c.Name←1⊃r
     c.HandshakeDone←0
     _←c SendMessage¨'SupportedProtocols=2' 'UsingProtocol=2' '["Identify",{"identity":1}]'
     c MessageHandler&⍬
 }
