 WaitFor←{
     c←⍺
     e t←2↑(⊆⍵),10
     c{
         i←(en←⊃¨⍺.Messages)⍳⊂e
         (i=≢en)∧⍵<0:¯1
         i=≢en:⍺ ∇ ⍵-⎕DL 0.5
         m←i 1⊃⍺.Messages
         ⍺.Messages↓⍨←i+1
         m
     }t
 }
